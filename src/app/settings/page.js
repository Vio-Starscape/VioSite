import Settings from "@/components/Settings/Index";

export const metadata = {
    title: "Vio - Settings",
    description: "Vio Settings Panel",
};

export default function Page() {
    return <Settings />;
}